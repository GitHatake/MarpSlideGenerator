# Marp Slide Generator

プロジェクトの成果物や設計資料から、Marpを活用して視認性が高く美しいプレゼンテーションスライドを自動生成するためのツールです。親プロジェクトにサブモジュールとして組み込んで使用することを前提に設計されています。

## 🚀 親プロジェクトへの導入手順 (サブモジュール)

あなたのプロジェクトにこのスライド生成ツールを導入する手順です。

1. **サブモジュールの追加**
   プロジェクトのルートディレクトリで、このリポジトリをサブモジュールとして追加します。
   ```bash
   git submodule add <このリポジトリのURL> slide-generator
   ```

2. **依存関係のインストール**
   追加されたディレクトリに移動し、必要なパッケージをインストールします。
   ```bash
   cd slide-generator
   npm install
   ```

3. **コミット**
   親プロジェクトに戻り、サブモジュールの追加をコミットします。
   ```bash
   cd ..
   git add .gitmodules slide-generator
   git commit -m "Add slide-generator submodule"
   ```

## 🤖 エージェント（Gemini等）を用いたスライド作成の自動化ワークフロー

AIエージェントにプロジェクトの資料を渡し、このツールを使ったスライド生成を指示する際のワークフローです。

### エージェントへの指示方法（プロンプト例）

親プロジェクトのルートディレクトリ等で、エージェントに対して以下のようなプロンプトを与えてください。これにより、エージェントは自動的に必要なJSONを生成し、スライドをビルドします。

```text
あなたはこのプロジェクトの資料をもとに、プレゼンテーションスライドを作成するエージェントです。

以下の手順で作業を行ってください。

1. 【ルールの確認】
   `slide-generator/prompts/system_prompt.md` の内容を読み込み、スライドデータのスキーマ定義と生成ルール（特に、出力すべき JSON フォーマットの制約）を完全に理解し、厳守してください。

2. 【情報の抽出とスライド構成の作成】
   プロジェクト内の以下のファイル（※スライド化したいファイルパスを指定）を読み込み、内容を分析してください。
   ・ `docs/requirements.md`
   ・ `README.md`
   
   分析した内容をもとに、上記で確認したルールに従って `slideData` （JSON配列）を生成してください。

3. 【ファイルの保存】
   生成したJSONデータを、親プロジェクトのルートディレクトリに `slide_input.json` というファイル名で保存してください。
   ※必ずプレーンなJSON形式で保存し、Markdownのコードブロック記法（```json など）は含めないでください。

4. 【スライドのビルド】
   保存が完了したら、`slide-generator` ディレクトリに移動し、以下のコマンドを実行してスライド（PDFおよびMarkdown）を生成してください。
   ```bash
   cd slide-generator && npm run generate
   ```

5. 【完了の報告】
   ビルドが成功したら、生成されたファイル（`slide-generator/build/output.pdf` など）の場所を私に報告してください。
```

## 🛠️ 手動での生成コマンド

エージェントを使わず手動で生成する場合は、以下の手順で行います。

1. `slide-generator` の1つ上の階層（親プロジェクトのルート）に、`slideData` 配列が記述された `slide_input.json` を配置します。
2. `slide-generator` ディレクトリ内で以下のコマンドを実行します。

```bash
# MarkdownとPDFを一括生成 (buildディレクトリに出力されます)
npm run generate

# プレビューしながら調整したい場合
npm run preview

# HTML形式で出力したい場合
npm run build:html

# PowerPoint形式(pptx)で出力したい場合
npm run build:pptx
```

## 🎨 スタイルのカスタマイズ
`themes/custom-theme.css` 内の `:root` セクションにあるカラーコード（`--color-primary` など）を変更するだけで、プロジェクトのブランドカラーに合わせたスライドデザインに即座に変更できます。
